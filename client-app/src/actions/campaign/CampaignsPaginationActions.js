export const setCampaignsPagination = pagination => ({
    type: 'SET_CAMPAIGNS_PAGINATION',
    pagination,
});

export const setCampaignPagination = pagination => ({
    type: 'SET_CAMPAIGN_PAGINATION',
    pagination,
});
