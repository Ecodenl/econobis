export const setFullNameFilter = (fullName) => ({
    type: 'SET_FULL_NAME_FILTER',
    fullName,
});

export const setRegistrationFilter = (registration) => ({
    type: 'SET_REGISTRATION_FILTER',
    registration,
});
